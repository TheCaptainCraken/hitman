use std::path::Path;

use std::io::Error;

pub fn move_files(source: &Path, destination: &Path, verbose: bool) -> Result<(), Error> {
    todo!();
}

pub fn delete_files(source: &Path, verbose: bool) -> Result<(), Error> {
    todo!();
}
